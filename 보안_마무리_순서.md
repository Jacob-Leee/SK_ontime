# 보안 마무리 순서

세 앱 모두 Google 로그인이 붙었습니다. 이제 데이터베이스 규칙을 조일 수 있는데,
**한 번에 다 잠그면 뭐가 깨졌는지 알 수 없으므로** 단계로 나눕니다.

각 단계 사이에 확인이 통과해야 다음으로 갑니다. 문제가 생기면 **직전 단계의
규칙을 다시 게시하면 즉시 원복**됩니다. 데이터는 어느 경우에도 건드리지 않습니다.

---

## 0단계 — 파일 배포 (지금)

올릴 파일: `index.html`, `mobile.html`, `subbie.html`

빌드 `2026-07-29-j`. 설정 화면 하단에서 확인할 수 있습니다.

### 이 단계에서 바뀌는 것

**모바일에도 로그인 화면이 생깁니다.** 이제 폰에서 앱을 열면 Google 로그인이
먼저 뜹니다. 허용 명단에 없는 계정은 들어올 수 없습니다.

### 확인

- [ ] 데스크톱: 로그인 후 정상 진입
- [ ] 폰: 로그인 화면이 뜨고, 로그인 후 진입
- [ ] 폰: 서랍에 이름이 표시됨
- [ ] 시크릿 창: 링크만으로 열면 로그인 화면

> **잠겼다면** — `mobile.html` 의 `AUTH_REQUIRED` 를 `false` 로 되돌려 다시 올리면
> 즉시 풀립니다. 파일에서 `AUTH_REQUIRED` 를 검색하면 한 곳만 나옵니다.

---

## 1단계 — 토큰·알림 큐 잠금

파일: `database.rules.STEP1.json`

Firebase Console → Realtime Database → **규칙** 에 붙여넣고 **게시**

### 잠기는 것

| 경로 | 변경 |
|---|---|
| `fcmTokens` 쓰기 | 개방 → **로그인 필요** |
| `notifications` 쓰기 | 개방 → **로그인 필요** |

오더 데이터(`orders`, `issues`)는 **아직 열어둡니다.** 한 번에 하나씩만 바꿔야
문제가 생겼을 때 원인을 특정할 수 있습니다.

### 확인 (게시 후 5분)

- [ ] 폰: 서랍 → 알림(Push) 껐다 켜기 → 오류 없이 등록됨
- [ ] 데스크톱: 오더 배정 → 상대 폰에 알림 도착
- [ ] 관리자: ⚙️ → 알림 → 등록 현황이 정상 조회됨

> 알림 등록에서 `permission_denied` 가 나면 그 기기가 로그인되지 않은 상태입니다.
> 로그인부터 확인하세요.

---

## 2단계 — 오더 데이터 잠금 (최종)

파일: `database.rules.SECURE.json`

**1단계가 며칠간 문제없이 돌아간 뒤에** 진행하세요.

### 잠기는 것

| 경로 | 변경 |
|---|---|
| `orders` | 개방 → **로그인 + 허용 명단** |
| `issues` | 개방 → **로그인 + 허용 명단** |

이 시점부터 **DB 주소를 알아도 데이터를 볼 수 없습니다.** 지금까지는 주소만
알면 누구나 읽고 쓸 수 있는 상태였습니다.

### 확인

- [ ] 세 앱 모두 정상 동작
- [ ] 협력업체 앱(`subbie.html`): 로그인 후 본인 오더만 보임
- [ ] 브라우저에서 `https://homes-nsw-default-rtdb.firebaseio.com/orders.json`
      직접 열기 → `Permission denied` 나오면 성공

---

## 원복 방법

| 상황 | 조치 |
|---|---|
| 앱에 못 들어감 | `AUTH_REQUIRED` 를 `false` 로 되돌려 재배포 |
| 알림 등록 실패 | `database.rules.json` (현재본) 재게시 |
| 오더가 안 보임 | `database.rules.STEP1.json` 재게시 |

규칙 게시는 **데이터를 변경하지 않습니다.** 접근 권한만 바뀌므로 되돌리면
그대로 복구됩니다.

---

## 아직 남은 것

### subbie.html 실제 테스트

협력업체용 Google 로그인을 붙였지만 **실제로 시험해보지 않았습니다.**
2단계 전에 협력업체 계정을 하나 만들어(역할: 협력업체) 확인하는 것이 안전합니다.

확인할 것:
- 로그인이 되는가
- 본인 오더만 보이는가
- 관리자 앱(`index.html` / `mobile.html`)에는 거부되는가

### 역할 정리

현재 5명 중 4명이 관리자입니다. 관리자는 **다른 사람 계정을 삭제하고 동기화
주소를 바꿀 수 있습니다.** 실수 한 번으로 팀 전체가 앱을 못 열게 될 수 있어
`SK_Jacob` 만 관리자로 두는 것을 권합니다.

⚙️ → 계정·보안 → 허용 계정 목록의 드롭다운에서 변경합니다.
데이터 접근 권한은 직원과 관리자가 완전히 동일합니다.
